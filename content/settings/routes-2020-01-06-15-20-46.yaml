routes:

collections:
  /:
    permalink: /{slug}/
    template: indexs
    filter: tag:-es
  /en/:
    permalink: /en/{slug}/
    template: indexs-es
    filter: tag:es

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
