routes:

collections:
  /:
    permalink: /{slug}/
    template: indexs
    filter: 'tag:-es'
  /es/:
    permalink: /es/{slug}/
    template: indexs-es
    filter: 'tag:es'

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
